// as_clkbuff12_bb.v — Yosys blackbox stub for gf180mcu_as_sc_mcu7t3v3__clkbuff_12
// Used to pre-insert a clock buffer in RTL so the placer (not CTS) positions it,
// avoiding DRT-0073 access-point failures on CTS-placed buffers.
(* blackbox *)
module gf180mcu_as_sc_mcu7t3v3__clkbuff_12 (
    input  I,
    output Z
);
endmodule
